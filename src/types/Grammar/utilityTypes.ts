export type Grammar = {
  grammarForLessonId: string;
  id: string;
  image: string;
  title: string;
};

export type GrammarPageContentful = {
  headerTitle: string;
  lessonType: string;
  nextStep: string;
};
